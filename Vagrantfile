Vagrant.configure("2") do |config|
  # Box configuration
  config.vm.box = "ubuntu/trusty64"
  # Provider settings
  config.vm.provider "virtualbox" do |vb|
    vb.memory = 2048
    vb.cpus = 4
  end
end
